{
  "name": "kkn-trader-backend",
  "version": "1.0.0",
  "description": "Production API for KKN TRADER - Professional Trading Education & Paper Trading Web Platform",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch-path=controllers --watch-path=routes --watch-path=services --watch-path=middleware --watch-path=models --watch-path=config --watch-path=server.js server.js",
    "seed": "node -e \"require('./services/seedService').seedDatabase()\""
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.4.1"
  }
}
